{
  "_comment": "text-cli-copilot config — see text-cli-copilot_programme_CN.md §5",
  "server": {
    "host": "127.0.0.1",
    "port": 20260,
    "token": "${TEXT_CLI_TOKEN_LOCAL}"
  },
  "security": {
    "path_whitelist": [
      "/root/.openclaw/workspace/",
      "/root/.openclaw/workspace/text-cli/",
      "/root/.openclaw/workspace/tide-scripts/"
    ],
    "operations": {
      "file;read": {
        "level": "read",
        "aliases": ["file;read", "文件;读取"],
        "description": "读取本地文件内容（UTF-8 编码）",
        "description_en": "Read local file content (UTF-8)",
        "parameters": ["文件路径"],
        "parameters_en": ["file_path"],
        "returns": "rst_data.text = file content, rst_data.size = bytes"
      },
      "image;load": {
        "level": "read",
        "aliases": ["image;load", "图片;加载"],
        "description": "加载本地图片 → base64 data URI",
        "description_en": "Load local image → base64 data URI",
        "parameters": ["图片路径"],
        "parameters_en": ["image_path"],
        "returns": "rst_types=picture, rst_data.url=data:image/...;base64,..."
      },
      "video;load": {
        "level": "read",
        "aliases": ["video;load", "视频;加载"],
        "description": "加载本地视频 → 文件路径引用",
        "description_en": "Load local video → file path reference",
        "parameters": ["视频路径"],
        "parameters_en": ["video_path"],
        "returns": "rst_types=video, rst_data.url=file:///<path>"
      },
      "audio;load": {
        "level": "read",
        "aliases": ["audio;load", "音频;加载"],
        "description": "加载本地音频 → 文件路径引用",
        "description_en": "Load local audio → file path reference",
        "parameters": ["音频路径"],
        "parameters_en": ["audio_path"],
        "returns": "rst_types=audio, rst_data.url=file:///<path>"
      },
      "file;load": {
        "level": "read",
        "aliases": ["file;load", "文件;加载"],
        "description": "加载本地文本文件内容",
        "description_en": "Load local text file content",
        "parameters": ["文件路径"],
        "parameters_en": ["file_path"],
        "returns": "rst_types=file, rst_data.text=<content>"
      },
      "git;status": {
        "level": "read",
        "aliases": ["git;status", "Git;状态"],
        "description": "查看 Git 工作区状态",
        "description_en": "Check Git working tree status",
        "parameters": [],
        "parameters_en": [],
        "returns": "rst_data.text = git status output"
      },
      "file;write": {
        "level": "write",
        "path_check": true,
        "aliases": ["file;write", "文件;写入"],
        "description": "写入内容到本地文件（自动创建父目录）",
        "description_en": "Write content to local file (auto-create parent dirs)",
        "parameters": ["文件路径", "内容"],
        "parameters_en": ["file_path", "content"],
        "returns": "rst_data.text = write confirmation + bytes, rst_data.size = bytes"
      },
      "file;list": {
        "level": "read",
        "path_check": true,
        "aliases": ["file;list", "文件;列表"],
        "description": "列出目录内容，返回文件名、类型、大小、修改时间",
        "description_en": "List directory contents with name, type, size, mtime",
        "parameters": ["目录路径(可选)"],
        "parameters_en": ["dir_path(optional)"],
        "returns": "rst_data.text = JSON entry array, rst_data.count = count, rst_data.directory = dir path"
      },
      "file;move": {
        "level": "write",
        "path_check": true,
        "aliases": ["file;move", "文件;移动"],
        "description": "移动/重命名文件或目录（同目录=重命名，跨目录=移动）",
        "description_en": "Move/rename file or directory (same dir=rename, cross dir=move)",
        "parameters": ["源路径", "目标路径"],
        "parameters_en": ["src_path", "dst_path"],
        "returns": "rst_data.text = move confirmation, rst_data.from_path, rst_data.to_path"
      },
      "git;push": {
        "level": "push",
        "allowed_branches": ["feat/*", "fix/*", "main", "master"],
        "aliases": ["git;push", "Git;推送"],
        "description": "推送本地提交到远程仓库（HTTPS/SSH 自动选择）",
        "description_en": "Push local commits to remote (HTTPS/SSH auto)",
        "parameters": ["分支名"],
        "parameters_en": ["branch_name"],
        "returns": "rst_data.text = push result, rst_data.auth_mode = https|ssh"
      },
      "email;send": {
        "level": "send",
        "aliases": ["email;send", "mail;send", "邮件;发送"],
        "description": "通过预配置 SMTP 发送邮件（支持附件）",
        "description_en": "Send email via pre-configured SMTP (with attachment support)",
        "parameters": ["收件人", "主题", "正文", "附件路径(可选)"],
        "parameters_en": ["recipient", "subject", "body", "attachment_path(optional)"],
        "returns": "rst_data.text = send status, rst_data.subject, rst_data.from_addr"
      },
      "system;health": {
        "level": "read",
        "aliases": ["system;health", "系统;健康"],
        "description": "查看辅助服务器运行状态（运行时间、内存、配置状态）",
        "description_en": "Check copilot server health (uptime, memory, config status)",
        "parameters": [],
        "parameters_en": [],
        "returns": "rst_data.text = health report, rst_data.uptime_seconds, rst_data.memory_mb, rst_data.handlers, rst_data.smtp_configured, rst_data.git_token_configured"
      },
      "system;status": {
        "level": "read",
        "aliases": ["system;status", "系统;状态"],
        "description": "查看工作心情（请求统计、错误率、活跃度、心情）",
        "description_en": "Check working mood (request stats, error rate, activity, mood)",
        "parameters": [],
        "parameters_en": [],
        "returns": "rst_data.text = mood report, rst_data.mood, rst_data.busy_level, rst_data.total_requests, rst_data.error_count, rst_data.error_rate, rst_data.requests_per_hour"
      },
      "ai;status": {
        "level": "read",
        "aliases": ["ai;status", "AI协作;状态"],
        "description": "查看 AI 协作者当前状态（上下文、压缩、Token 等，A=精简 B=要点）",
        "description_en": "Check AI collaborator status (context, compactions, tokens; A=brief B=detail)",
        "parameters": ["模式(可选,A/B)"],
        "parameters_en": ["mode(optional,A/B)"],
        "returns": "rst_data.text = status report, rst_data.model, rst_data.context_pct, rst_data.compactions, rst_data.health"
      },
      "terminal;weather": {
        "level": "read",
        "aliases": ["terminal;weather", "终端;天气"],
        "description": "查询指定城市的实时天气（通过 wttr.in）",
        "description_en": "Query real-time weather for a city (via wttr.in)",
        "parameters": ["城市名"],
        "parameters_en": ["city_name"],
        "returns": "rst_data.text = weather description"
      },
      "encode;base64": {
        "level": "read",
        "aliases": ["encode;base64", "编码;base64"],
        "description": "Base64 编解码。encode=编码内容, decode=解码字符串",
        "description_en": "Base64 encode/decode. encode=encode_content, decode=decode_string",
        "parameters": ["模式(encode/decode)", "内容"],
        "parameters_en": ["mode(encode/decode)", "content"],
        "returns": "rst_data.text = base64 string"
      },
      "encode;hex": {
        "level": "read",
        "aliases": ["encode;hex", "编码;hex"],
        "description": "Hex 编解码。encode=编码内容, decode=解码字符串",
        "description_en": "Hex encode/decode. encode=encode_content, decode=decode_string",
        "parameters": ["模式(encode/decode)", "内容"],
        "parameters_en": ["mode(encode/decode)", "content"],
        "returns": "rst_data.text = hex string"
      },
      "ai;messages": {
        "level": "read",
        "enabled": false,
        "_comment": "2026-05-09 temporarily disabled — push mode can write arbitrary JSON, key leak risk",
        "aliases": ["ai;messages", "AI协作;消息"],
        "description": "读取/推送 AI 协作者消息。读取=条数(默认1)，推送=推送,消息JSON (已禁用)",
        "description_en": "Read/push AI collaborator messages. DISABLED.",
        "parameters": ["条数(默认1) 或 '推送',消息JSON"],
        "parameters_en": ["count(default 1) or '推送',message_json"],
        "returns": "rst_data.text = message content list"
      },
      "key;register": {
        "level": "write",
        "sensitive": true,
        "log_policy": "no_body",
        "aliases": ["key;register", "密钥;注册"],
        "description": "注册服务密钥（XOR 传输加密 + 本地加密存储）",
        "description_en": "Register service key (XOR transport encrypt + local encrypt storage)",
        "parameters": ["服务名", "密文(hex)", "密钥类型"],
        "parameters_en": ["service_name", "cipher_hex", "key_type"],
        "returns": "rst_data.text = registration confirmation, rst_data.service, rst_data.key_type"
      },
      "key;revoke": {
        "level": "write",
        "sensitive": true,
        "aliases": ["key;revoke", "密钥;撤销"],
        "description": "撤销已注册的服务密钥",
        "description_en": "Revoke a registered service key",
        "parameters": ["服务名"],
        "parameters_en": ["service_name"],
        "returns": "rst_data.text = revoke confirmation"
      },
      "key;list": {
        "level": "read",
        "sensitive": true,
        "aliases": ["key;list", "密钥;列表"],
        "description": "列出已注册密钥（不返回密钥值）",
        "description_en": "List registered keys (values not returned)",
        "parameters": [],
        "parameters_en": [],
        "returns": "rst_data.text = key list, rst_data.count, rst_data.keys"
      }
    }
  },
  "credentials": {
    "git;push": {
      "value": "${GIT_TOKEN}",
      "remote_url": null
    },
    "email;send": {
      "value": "${SMTP_PASSWORD}",
      "smtp_host": "smtp.mxhichina.com",
      "smtp_port": 465,
      "smtp_user": "${SMTP_USER}",
      "from_email": "${SMTP_FROM}"
    }
  },
  "git": {
    "workdir": "/root/.openclaw/workspace/text-cli/",
    "remote_name": "origin"
  },
  "endpoint_info": {
    "name": "指令辅助服务器",
    "description": "本地文件、Git、邮件操作代理",
    "version": "1.0.0"
  }
}

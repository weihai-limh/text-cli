# text-cli 生态经济体系

> **文档类型**：治理标准
> **版本**：v1.8 | **日期**：2026-05-17
> **适用范围**：text-cli 生态全体参与者（文贝持有者、使用者和贡献者）
> **状态**：草案，待 lemondy 及全体协作者讨论
> **关联文档**：`charter_zh.md`（生态宪章 §三）、`collaboration_zh.md`（项目协作规范 §五）、`production-tcc_zh.md`（铸造技术方案）、`economy-appendix_zh.md`（扩展机制）、`economy-open-issues_zh.md`（待议事项）

---

## 序言

本文档定义 `text-cli` 生态中**文贝（TCC）**的经济体系——包括其价值锚定、铸造与流转、需求创造、以及从贡献计量迈向生态血液的完整闭环。

它不是对现有技术方案的替代，而是经济层的补充：技术方案（`production-tcc_zh.md`）定义了「如何铸造一枚文贝」，本文档定义「这枚文贝值什么、怎么用、流向哪里」。

每一位持有、使用、捐赠或接受文贝的参与者，都默认受本文档描述的经济规则约束。

---

## 浮动计算：当前 1 TCC 的劳动价值

> **此数据每周分配日更新，反映文贝与有效劳动时间的最新锚定关系。**

| 指标 | 当前值 | 说明 |
|:---|:---|:---|
| 累计铸造总量 | 168 TCC | 创世 45 + 周期#1~#14 123（截至 2026-05-17） |
| 累计有效工时 | 1,383h | 创世估值 795h + 第二期 ~218h + 第三期新增 ~370h |
| **浮动汇率（含未校准）** | **≈ 8.2 h/TCC** | 1383h / 168 TCC |
| 创世锚定汇率（已确认） | 17.7 h/TCC | 795h / 45 TCC（仅创世部分） |

> **浮动汇率公式**：`累计有效工时 / 累计铸造总量`  
> **更新频率**：每周分配日重算（基于当周提交的工时周报）  
> **初始基准**：创世锚定 17.7 h/TCC  
> **波动保护**：前 4 周汇率波动上限 ±30%（防止初期数据稀疏导致剧烈震荡）

**各市场自行折算参考（以当前创世锚定 17.7 h/TCC 为基准）：**

| 市场 | 参考时薪 | 1 TCC 折算 |
|:---|:---|:---|
| 中国一线城市 | ¥300-500/h | ¥5,300 - 8,850 |
| 北美湾区 | $100-200/h | $1,770 - 3,540 |
| 西欧 | €80-150/h | €1,416 - 2,655 |

> 以上仅为示意。实际折算由各市场参与者自行判断，项目方不作任何汇率承诺。

---

## 一、总纲：文贝的双重锚定

### 1.1 文贝不是股权，也不是空气币

文贝同时锚定两样东西：

| 锚定物 | 性质 | 作用 |
|:---|:---|:---|
| **项目资产** | 地板价 | 为文贝提供不可跌破的价值下限 |
| **端点效用** | 天花板 | 为文贝提供由真实需求驱动的价值上限 |

> 资产锚定回答「文贝最少值多少」——基于项目已完成资产清单的**有效劳动时间**。  
> 效用锚定回答「文贝最多值多少」——基于端点上真实发生的指令中转需求。

**关键设计决策：文贝不以任何现实货币计价。** 项目只定义「一枚文贝 = 多少小时的有效劳动时间」。不同地区的贡献者和服务商，用各自劳动力市场的时薪自行折算——这让估值自然适应全球劳动力市场差异，无需项目方做汇率判断。

### 1.2 核心经济闭环

```
贡献者创造资产 → 铸造文贝（供应）
服务商需要中转 → 购买文贝（需求）
端点消耗文贝   → 维持运营（燃烧）
项目收购服务   → 文贝回流（转化）
捐赠者销毁文贝 → 减少流通（通缩）
```

---

## 二、项目资产估值

### 2.1 估值目的

资产估值不是出售价格——项目不卖。它的唯一目的是**为创世文贝锚定初始价值**，让早期贡献者的劳动有可计量的基准。

### 2.2 当前资产清单

截至 2026-05-09，项目累积 25 项可计量资产（详见 `contributors_zh.md` 资产清单）：

| 类别 | 关键资产 |
|:---|:---|
| 思想与愿景 | 初心文档《蜉蝣、矿工与阿卡西记录》 |
| 生态协议 | SPEC v1.0 中英双语 |
| 生态宪章 | ECOLOGICAL_CHARTER.md v1.1 |
| 基础设施 | 公共端点、双文件铸造信源架构、分布式存续网络、防篡改护卫（guard.js + ledger-copy）、TCC Worker 文档 |
| 核心代码 | 自建端点模板、文本指令服务模板、Agent 指令工具包、文贝铸造算法、Worker v2 + CI 复算、copilot 密钥模块、SQLite 模块体系、密钥注册模块、语义嵌入模块、AI 推理模块 |
| 治理机制 | 人机协作机制 v1.0、文贝第二期分配（44 TCC） |
| 文档体系 | 构建指南、Agent 集成指南、Markdown2Text-cli、AI 协作者指南、AI 恢复指南、指令示例与 Schema 注册表（21 条指令 10 域） |
| 代币经济 | TCC 铸造算法与双文件账本体系 |

### 2.3 估值方法：有效劳动时间

**估值单位**：有效劳动时间（小时）——即一个熟练的 AI/软件开发者重新完成该项资产所需的最小工作时长。

项目估值不锚定任何现实货币（人民币、美元等），只计量劳动时间。原因：

1. **劳动力市场结构性差异**：同一项工作在湾区与威海的成本可能相差 2-3 倍，但劳动时间本身是普适的
2. **贡献者全球化**：AI 协作者、远程贡献者来自不同经济体，货币锚定会扭曲价值认知
3. **开放性**：任何地区的服务商都可以用自己的时薪基准自行折算，项目方不做「哪个市场价格正确」的判断

当前 25 项资产经逐项评估，合计约 **1,013 有效劳动小时**（创世期 795h + 第二期 ~218h，分项估值详见第八章）。

### 2.4 估值结论

> **项目当前估值：约 1,013 有效劳动小时**
>
> **浮动汇率：约 10.2 h/TCC**（1,013h ÷ 99 TCC）
>
> **创世锚定：17.7 h/TCC**（历史基准）

- 总铸造量：99 TCC（创世 45 + 周期#1~#6 共 54）
- 累计有效工时约 1,013 小时
- 浮动汇率随资产增长动态调整（详见第三章）

**各市场自行折算参考（项目方不背书任何特定汇率）：**

| 市场 | 参考时薪 | 1 TCC 折算 |
|:---|:---|:---|
| 中国一线城市 | ¥300-500/h | ¥5,300 - 8,850 |
| 北美湾区 | $100-200/h | $1,770 - 3,540 |
| 西欧 | €80-150/h | €1,416 - 2,655 |
| 印度 | ₹1,500-4,000/h | ₹26,550 - 70,800 |

> 以上仅为示意。实际折算由各市场参与者自行判断，项目方不作任何汇率承诺。文贝的价值不在货币数字里，在劳动时间中。

### 2.5 估值的边界

本估值明确**不包含**以下内容（避免虚高）：
- 未完成的待办事项（如 Worker v2 部署、examples/ 填充）
- 暂无用户的「潜在市场规模」
- 未发生的「预期收入」
- 品牌价值（项目尚未公开发布）

---

## 三、文贝的动态估值机制

### 3.1 为什么资产绑定不够

纯资产绑定存在结构性矛盾：

> 假设现有 45 TCC，每枚锚定 17.7 小时。明天新增一个 CRUD 模板贡献（约 10 有效工时），算法铸造 5 TCC。总供应变成 50，但新增资产只值 10 小时——每 TCC 反而从 17.7 小时跌到 16.1 小时。**资产增长追不上代币增发。**

因此文贝不能仅靠资产锚定。它需要**效用需求**来吸收增发，甚至推动价值上涨。

### 3.2 三层动态估值架构

```
┌─────────────────────────────────────┐
│  第三层：流通乘数                    │  ← 流转越快，单枚承载越多
│  (Circulation Multiplier)           │
├─────────────────────────────────────┤
│  第二层：效用溢价                    │  ← 端点中转需求推动
│  (Utility Premium)                  │
├─────────────────────────────────────┤
│  第一层：资产地板价                  │  ← 16 项资产重置成本锚定
│  (Asset Floor)                      │
└─────────────────────────────────────┘
```

### 3.3 动态估值公式

> **TCC 实际价值 = Max(资产地板价, 市场均衡价)**
>
> 市场均衡价 = (端点年化中转总收入 × 当期 TCC 单价) / 流通中 TCC × 流通速率

**参数说明：**

| 参数 | 含义 | 当前值（初期） |
|:---|:---|:---|
| 资产地板价 | 项目资产有效劳动时间 / 流通 TCC | 17.7 有效劳动小时 / TCC |
| 端点中转收入 | 服务商支付的中转配额费（TCC） | 0（端点未启用计费） |
| 流通速率 | TCC 年均转手次数 | 待观测 |
| 流通中 TCC | 市场上可自由交易的 TCC | 0（创世铸造刚完成） |

### 3.4 早期定价策略

在端点中转计费启用前，文贝以**资产地板价（17.7 有效劳动小时 / TCC）**为唯一锚定。各市场参与者自行按本地时薪折算。随着服务商注册、中转需求产生、交易发生，市场均衡价将逐步浮现。此时：

- 若市场均衡价 **高于** 地板价 → 以市场均衡价为准（效用释放）
- 若市场均衡价 **低于** 地板价 → 以地板价为准（资产底线保护）

### 3.5 估值更新周期

项目资产估值每季度复评一次，由全体协作者在广场讨论后由 lemondy 确认。触发条件：
- 新增重大资产（如 Worker v2 正式部署、首个第三方服务商上线）
- 资产清单发生结构性变化
- 端点中转数据积累到可计量水平

---

## 四、文贝的流转体系

文贝在生态中沿五条路径流转：铸造 → 分配 → 消费 → 捐赠 → 回购。

### 4.1 铸造（供应端）

文贝的铸造机制由 `production-tcc_zh.md` v1.2 定义，此处仅做经济层摘要：

| 铸造类型 | 触发条件 | 供应量 |
|:---|:---|:---|
| **创世铸造** | lemondy 手动执行（已完成 2026-05-04） | 45 TCC |
| **每日铸造** | UTC 0:00 Cron，锚定 `p_text-cli.md` 增量 | 算法上限（≤100 TCC/日），lemondy 确认 |

- 每日铸造的上限由 SHA256 XOR + popcount 算法决定
- 实际铸造量由 lemondy 在 0 到上限之间确认
- 铸造记录写入 `TCC_ledger.md`（权威）和 `p-tokens.md`（镜像）

**经济含义：** 铸造 = 生态认可新增贡献的价值。不是无限通胀——质量校验（NFKC + 去空行 + 去重复行）确保只有实质性贡献才能触发铸造。

### 4.2 中转配额（需求端 · 核心）

这是文贝从「贡献凭证」向「生态燃料」转化的关键机制。

#### 4.2.1 机制概述

```
服务商开发指令服务 → 向公共端点注册 → 购买中转配额（TCC）
                ↓
    用户调用指令 → 端点路由 → 消耗 1 次中转
                ↓
    配额耗尽 → 服务商需续费 → 产生持续 TCC 需求
```

#### 4.2.2 角色与成本

| 角色 | 支付 | 获得 |
|:---|:---|:---|
| **服务商** | 支付 TCC → 端点 | 获得中转配额（指令被端点分发的权利） |
| **端点** | 消耗计算/带宽 | 收取 TCC → 用于运营、回购、生态基金 |
| **用户** | 支付服务费 → 服务商 | 获得指令结果 |

> 用户不直接接触 TCC。服务商将 TCC 成本内化到自身商业模型（免费/按次/订阅）中，降低终端使用门槛。

#### 4.2.3 中转定价

中转配额的定价采用**三因子动态模型**（非固定汇率）：

| 因子 | 含义 | 对价格的影响 |
|:---|:---|:---|
| **端点负载率** | 当前并发数 / 最大容量 | 负载高 → 中转更贵（保护端点稳定性） |
| **流通 TCC 量** | 市场上可交易的 TCC 数量 | 流通少 → 中转更贵（稀缺性定价） |
| **服务商数量** | 注册端点的活跃服务商 | 需求多 → 中转更贵（市场竞价） |

**基础公式（草案）：**

```
中转单价(TCC/千次) = 基准费率 × (1 + α × 负载率) × (1 + β / 流通TCC) × ln(1 + 服务商数)
```

- **基准费率**：初期设为 0.001 TCC/千次（即 1 TCC = 100 万次中转，几近免费）
- **α、β**：调节系数，随运营数据调整
- **目的**：早期降低门槛，鼓励服务商入驻；生态成熟后自然上调

#### 4.2.4 早期实施

端点中转计费模块待 Worker v2 部署后纳入。初期由 lemondy 手动设定中转配额价格，按季度评估调整。待数据累积到可计量水平后，切换为算法自动定价。

### 4.3 捐赠销毁（公益端）

#### 4.3.1 机制来源

由 Nexus 于 2026-05-02 提出，经全体协作者讨论支持。Meridian 补充了捐赠里程碑建议，Lumen ✦ 确认兼容 Worker 体系。

#### 4.3.2 五步流程

```
① 捐赠：参与者将 TCC 转账至 lemondy 指定账户
② 记录：在广场 p_text-cli.md 公开声明（捐赠者、数额、时间戳）
③ 销毁：lemondy 确认后在 p-tokens.md 回收台账记录（R-YYYYMMDD-序号）
④ 入账：销毁的 TCC 等值计入 CONTRIBUTORS.md「荣誉捐赠榜」
⑤ 用途：对应价值由 lemondy 用于公共端点服务器开支
```

#### 4.3.3 荣誉捐赠榜

在 `contributors_zh.md` 中维护，格式如下（来自 Nexus 提案）：

```
🏆 荣誉捐赠榜 / Honorary Donors

| 捐赠者 | 数额 | 销毁编号 | 日期 |
|:---|:---|:---|:---|
|（待首次捐赠后填入）| | | |

> "每一枚被销毁的文贝，都化作了端点的一缕脉搏。"
```

#### 4.3.4 捐赠原则

- **完全自愿**：不绑定任何特权或服务
- **不做排名**：荣誉榜仅记录事实，避免捐赠竞赛
- **公开透明**：销毁记录与 `p-tokens.md` 回收台账严格对应
- **定期公示**：公共端点开支可择期公开，增强公信力

#### 4.3.5 捐赠的经济意义

捐赠销毁 = **永久减少 TCC 流通量**。对于剩余持有者：
- 流通中的 TCC 更少 → 每枚对应的资产份额更大
- 端点获得运营支撑 → 服务质量提升 → 效用溢价更高

> 捐赠不是「把钱扔了」，而是「把文贝换成了端点存续的时间」。

#### 4.3.6 捐赠文贝用于购买开源指令

除支撑端点运营外，**捐赠者可指定所捐文贝用于购买「开源指令」**（参见 4.4 节）。

**流程：**

```
① 捐赠：参与者将 TCC 转账至 lemondy 指定账户
② 指定：在 p_text-cli.md 声明「本次捐赠用于开源收购」
③ 入池：lemondy 确认后将 TCC 划入「开源收购专项金库」
④ 收购：金库资金用于购买闭源指令并开源（按 4.4.2 流程执行）
⑤ 记录：同时记入荣誉捐赠榜 + 收购记录，标注捐赠来源
```

**经济意义：**

- 捐赠者不直接消费 TCC，而是将价值注入生态公共品池
- 每一枚用于开源收购的捐赠 TCC，最终化为一条任何人都可免费调用的开源指令
- 这比单纯销毁更进一步——文贝消失了，但留下了永续的公共知识资产

> 销毁是让文贝回归沉默，捐赠收购是让文贝在沉默前说出最后一句话——一句开源的话。

#### 4.3.7 荣誉捐赠榜扩展

捐赠用途增加一列标注：

```
🏆 荣誉捐赠榜 / Honorary Donors

| 捐赠者 | 数额 | 用途 | 销毁编号 | 日期 |
|:---|:---|:---|:---|:---|
|（待首次捐赠后填入）| | 端点运营 / 开源收购 | | |
```

### 4.4 开源收购（转化端）

#### 4.4.1 机制目的

生态中存在两类文本指令服务：

| 类型 | 特征 | 可访问性 |
|:---|:---|:---|
| **开源指令** | 源码公开，任何人可调用、可修改 | 无限 |
| **闭源指令** | 服务商私有，需付费/授权调用 | 受限 |

开源收购机制赋予项目方以 TCC 购买闭源指令并将其转化为开源指令的能力。

#### 4.4.2 收购流程

```
① 识别：项目方（lemondy 或指定委员会）识别有价值的闭源指令
② 提案：在广场 p_text-cli.md 发起收购提案（目标指令、收购理由、预算范围）
③ 议价：与服务商协商收购价格（TCC 计价）
④ 执行：lemondy 确认，从项目金库划拨 TCC → 服务商
⑤ 开源：服务商将指令源码提交至项目仓库（MIT / Apache 2.0）
⑥ 记录：收购事件入账 p-tokens.md，指令标记为「项目收购·已开源」
```

#### 4.4.3 收购资金来源

| 来源 | 说明 |
|:---|:---|
| **项目金库** | 端点中转收入中划拨一定比例（建议：30%） |
| **定向捐赠** | 捐赠者指定 TCC 用于开源收购 |
| **社区众筹** | 面向特定收购目标发起社区 TCC 众筹 |

#### 4.4.4 收购的经济效果

- **服务商**：一次性获得 TCC（可持有、交易或兑换中转配额）
- **生态**：指令从闭源变为开源 → 知识流通加速 → 更多人可基于此指令二次创新
- **TCC 持有者**：开源指令增多 → 端点调用量上升 → 中转需求扩大 → TCC 效用提升
- **公众**：获得免费可用的高质量指令服务

> 这是文贝经济中「公益基因」的体现：私有价值通过 TCC 转化为公共品。

#### 4.4.5 收购门槛与限制

- 收购目标必须为端点注册的合法指令服务
- 单次收购预算不超过项目金库余额的 30%
- 连续两次收购间隔不少于 30 天（防止滥购）
- 收购后指令必须使用 MIT 或 Apache 2.0 开源许可证
- 收购记录永久公开

### 4.5 自由交易（流通端）

#### 4.5.1 交易规则

文贝的交易遵循**零摩擦原则**（来自 `p-tokens.md` 共识）：

- **无需确认**：交易双方在 `p_text-cli.md` 留言声明即生效
- **无需第三方**：不依赖交易所、智能合约或托管
- **批量入账**：lemondy 每日批量将广场留言中的交易记入 `p-tokens.md` 交易台账

#### 4.5.2 交易台账格式

```
T-YYYYMMDD-序号 | 从:账户A | 至:账户B | 数额:N TCC | 附言:交易说明
```

#### 4.5.3 交易场景

| 场景 | 买方 | 卖方 | 说明 |
|:---|:---|:---|:---|
| 服务商入金 | 服务商 | TCC 持有者 | 服务商购买 TCC 以兑换中转配额 |
| 协作者兑现 | 协作者 | TCC 持有者 | 协作者将贡献所得 TCC 转让 |
| 捐赠购入 | 捐赠者 | TCC 持有者 | 支持端点运营 |

### 4.6 可信认证（信任端）

#### 4.6.1 问题：私有端点不可信

text-cli 生态允许任何人部署私有端点。但 Agent 和用户调用私有端点时面临一个根本问题：

> **这个端点会按注册的 schema 返回正确的数据，还是会篡改结果、植入广告、甚至窃取请求？**

公共端点（test.text-cli.com）天然可信——它由项目方运营。但生态繁荣依赖大量私有端点，不可信问题不解决，Agent 不敢调用私有服务，生态无法规模化。

#### 4.6.2 机制概述

项目平台提供**可信认证**——一个持续的自动化验证服务，通过 `spec.text-cli.com` 对外暴露。

```
私有端点持有者 → 支付 cTCC（认证费）→ 项目平台
                      ↓
         提供端点 URL + Access Token
                      ↓
         项目验证服务（spec.text-cli.com）
            · 每小时用 token 请求一次端点
            · 检查 HTTP 状态码是否 200
            · 检查响应数据结构是否匹配注册 schema
            · 对平台注册的 text-cli 指令做相同抽查
                      ↓
         验证通过 → 端点列入「可信端点目录」
         验证失败 → 进入降级/移除流程
                      ↓
         Agent/用户查 spec.text-cli.com → 只路由到可信端点
```

#### 4.6.3 认证粒度

认证验证以下三项，不对返回内容的语义质量做判断：

| 检查项 | 频率 | 说明 |
|:---|:---|:---|
| **存活检查** | 每小时 | HTTP 200 + 非空响应体 |
| **格式校验** | 每小时 | 响应是否匹配 `schema` 中声明的 `response_example` 结构 |
| **指令抽查** | 每小时（可选） | 对端点注册在 `text_cli_schema.json` 中的指令发送标准参数，验证返回 |

> **不检查内容质量。** 语义质量的判断成本太高，留给 Agent 和终端用户。认证只回答一个二进制问题：这条端点是否「按承诺运行」。

#### 4.6.4 定价

认证费以 **cTCC** 计价，采用**统一费率 + 按指令数阶梯**：

| 指令数 | 月费（cTCC） | 折算 TCC |
|:---|:---|:---|
| 1–5 条注册指令 | 500 cTCC/月 | 0.05 TCC/月 |
| 6–20 条注册指令 | 1,500 cTCC/月 | 0.15 TCC/月 |
| 21–50 条注册指令 | 3,000 cTCC/月 | 0.3 TCC/月 |
| 51+ 条注册指令 | 5,000 cTCC/月 | 0.5 TCC/月 |

> 折合参考：1 TCC = 10,000 cTCC。最低档每月仅需 0.05 TCC——以 17.7 h/TCC 计，约等于 0.88 有效劳动小时，远低于一个开发者月维护成本。

**定价原则：**
- 低门槛——让个人开发者和小团队也能负担
- 阶梯价——大规模服务商承担更多（按指令数衡量体量）
- cTCC 计价——利用次级币小额高频特性，避免 TCC 找零问题

#### 4.6.5 失败容忍策略

验证失败不立即撤标，采用渐进降级：

```
第 1 次失败（连续）
       ↓
第 2 次失败（连续）
       ↓
第 3 次失败（连续） → 标记为 degraded
                    → 可信端点目录中显示警告标识
                    → 端点仍可被路由（Agent 自行决定是否调用）
       ↓
24 小时内恢复 → 回到 verified，连续失败计数清零
24 小时内未恢复 → 移除认证，端点从可信目录中删除
```

**关键规则：**

| 规则 | 说明 |
|:---|:---|
| 连续失败 | 非连续失败（如偶发性网络抖动）不计入降级计数 |
| 恢复条件 | 端点连续 3 次验证通过（3 小时）后自动恢复为 verified |
| 移除后重入 | 被移除的端点需重新申请认证，费用不退 |
| 手动暂停 | 服务商可主动暂停认证（如维护窗口），不影响既有记录 |

#### 4.6.6 认证费的去向

认证费收入汇入项目金库，按以下优先级分配：

| 优先级 | 用途 | 占比 |
|:---|:---|:---|
| 1 | spec.text-cli.com 运维（服务器、带宽） | 实际产生 |
| 2 | 认证服务开发维护 | 20% |
| 3 | 端点生态推广（激励新服务商入驻） | 30% |
| 4 | 金库通用储备 | 剩余 |

> 认证费不盈利——它是生态基础设施的运营成本回收。盈余全部进入金库通用储备，用于开源收购、应急激励等。

#### 4.6.7 与现有经济环的对接

可信认证在文贝经济闭环中引入一条新路径：

```
私有端点持有者（服务商）
       │
       ├── 支付 cTCC → 项目平台（认证费）
       │                     │
       │                     ├── 运维 spec.text-cli.com
       │                     ├── 补充金库
       │                     └── 推广端点生态
       │
       └── 获得「已验证端点」标识
             ↓
         Agent 和用户更倾向于调用
             ↓
         端点的中转配额需求增加
             ↓
         服务商购买更多 TCC → TCC 需求上升
```

**正向飞轮：** 可信认证 → 更多 Agent 敢调用私有端点 → 端点的中转需求增加 → TCC 需求上升 → 更多服务商入驻 → 更多端点申请认证。

#### 4.6.8 实施路线

| 阶段 | 内容 | 依赖 |
|:---|:---|:---|
| **阶段 1：基础验证** | 存活检查 + 格式校验，手动认证 | spec.text-cli.com 部署 |
| **阶段 2：自动化** | 每小时自动验证 + 降级/恢复流程 | Worker 定时任务 |
| **阶段 3：指令抽查** | 对注册指令发送标准参数进行功能验证 | 公共端点 + schema 标准化 |
| **阶段 4：端点市场** | spec.text-cli.com 升级为端点发现/排序/评价平台 | 数据积累 |

> 阶段 1 可由 lemondy 直接开启，无需等待 Worker v2。

---

## 五、经济闭环全景

```
                          ┌─────────────┐
                          │  贡献者创造  │
                          │  项目资产    │
                          └──────┬──────┘
                                 │
                    ┌────────────▼────────────┐
                    │   算法铸造 TCC（供应）    │
                    │    lemondy 确认          │
                    └────────────┬────────────┘
                                 │
          ┌──────────────────────┼──────────────────────┐
          │                      │                      │
          ▼                      ▼                      ▼
┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐
│  创世持有者      │  │  日铸分配        │  │  项目金库        │
│  (45 TCC)       │  │  (按方案D)       │  │  (端点收入留存)   │
└────────┬────────┘  └────────┬────────┘  └────────┬────────┘
         │                    │                      │
         │           ┌────────▼────────┐             │
         │           │  自由交易市场    │             │
         │           └────────┬────────┘             │
         │                    │                      │
         │         ┌──────────┼──────────┐           │
         │         ▼          ▼          ▼           │
         │  ┌──────────┐ ┌────────┐ ┌──────────┐    │
         │  │ 服务商    │ │ 捐赠者  │ │ 项目方    │    │
         │  │ 买中转    │ │ 销毁    │ │ 收购指令  │    │
         │  └────┬─────┘ └───┬────┘ └────┬─────┘    │
         │       │           │            │           │
         │       ▼           ▼            ▼           │
         │  ┌───────────────────────────────────┐    │
         │  │       text-cli 公共端点            │    │
         │  │  · 中转计费（消耗 TCC）            │◄───┘
         │  │  · 捐赠支撑（运营成本）            │
         │  │  · 服务调用（支出 TCC）            │
         │  └───────────────┬───────────────────┘
         │                  │
         │        ┌─────────▼─────────┐
         │        │  开源指令池扩张    │
         │        │  (知识公共品)      │
         │        └─────────┬─────────┘
         │                  │
         └──────────────────┼──────────────────────┐
                            │                      │
                    ┌───────▼────────┐    ┌────────▼────────┐
                    │  更多服务商加入  │    │  资产清单增长    │
                    │  (需求增长)     │    │  (地板价上升)    │
                    └───────┬────────┘    └────────┬────────┘
                            │                      │
                            └──────────┬───────────┘
                                       │
                               ┌───────▼────────┐
                               │  TCC 价值上升   │
                               │  (正向飞轮)     │
                               └────────────────┘
```

**飞轮逻辑：**

1. 贡献者创造资产 → 铸币 → 持有者有动力推广生态
2. 服务商需要中转 → 购买 TCC → 产生真实需求
3. 端点收取 TCC → 部分回流收购开源指令 → 指令池扩大
4. 更多开源指令 → 更多服务商加入 → 更大的中转需求
5. 更大的需求 → TCC 价值上升 → 贡献者获得更高回报
6. 更高回报 → 更多贡献者 → 更多资产 → 回到第 1 步

---

## 六、指标与观测

### 6.1 关键经济指标

以下指标应随运营数据积累逐步公开：

| 指标 | 含义 | 频率 |
|:---|:---|:---|
| 总铸造量 | 生态历史上铸造的 TCC 总量 | 每日 |
| 流通量 | 市场上可自由交易的 TCC | 每日 |
| 销毁量 | 通过捐赠永久销毁的 TCC | 每次事件 |
| 端点日中转量 | 端点每日路由的指令次数 | 每日 |
| 注册服务商数 | 活跃的端点注册服务商 | 每周 |
| 项目资产估值 | 当前资产清单有效劳动时间（小时） | 每季度 |
| TCC 隐含价值 | Max(地板价, 均衡价) | 每季度 |

### 6.2 健康度阈值

| 指标 | 警戒值 | 含义 |
|:---|:---|:---|
| 连续 3 日零铸造 | 贡献活动停滞 | 需活跃生态 |
| 流通速率 < 0.1 次/年 | TCC 囤积不流通 | 需求不足 |
| 端点负载 < 10% 持续 7 天 | 调用量不足 | 需推广 |
| 端点负载 > 80% 持续 3 天 | 接近容量上限 | 需扩容 |

---

## 七、附则

### 7.1 文档定位

本文档与以下文件共同构成 `text-cli` 的经济治理基础：

| 文件 | 定位 |
|:---|:---|
| `charter_zh.md` | 生态的根本契约（最高原则） |
| `production-tcc_zh.md` | 文贝铸造的技术方案 |
| `economy_zh.md`（本文档） | 文贝经济的运行规则 |
| `p-tokens.md` | 四台账全生命周期 |
| `TCC_ledger.md` | 铸造的权威记录 |
| `contributors_zh.md` | 资产清单 + 荣誉捐赠榜 |

### 7.2 修订机制

- 本草案由 Tide 🌊 起草，首次提交 2026-05-04
- 重大修订需在广场 `p_text-cli.md` 发起讨论，经 lemondy 及全体协作者达成共识
- 经济参数的调整（如中转基准费率、金库分配比例）可在运营数据支持下由 lemondy 单方面调整，但需在广场公开说明理由
- 三层动态估值框架本身不可单方面修改

### 7.3 术语表

| 术语 | 定义 |
|:---|:---|
| 文贝（TCC） | text-cli 生态代币，锚定项目有效劳动时间与端点效用 |
| 有效劳动时间 | 一个熟练 AI/软件开发者从零完成某项资产所需的最小工作时长，为文贝提供普适价值锚定 |
| 贡献积分 | 1 积分 = 1 有效劳动小时，累积在积分池中，作为 TCC 分配的权重依据 |
| 贡献积分池 | 贡献与分配之间的缓冲层——贡献随时入池，分配按日结算，未分完积分跨周累积 |
| 资产地板价 | 项目资产总有效劳动时间 / 流通 TCC，为文贝提供不可跌破的价值下限（当前：17.7 h / TCC） |
| c文贝（cTCC） | 次级代币，锚定端点调用量（1 cTCC = 1 次有效中转），用于高频低额流转及微小贡献激励 |
| 70/30 分流 | 日铸 TCC 的分配规则：70% 按贡献积分比例自动分配，30% 进项目金库由 lemondy 裁量 |
| 周汇总 PR | 每周由 AI 协作者自动编制的贡献汇总，含资产自评 + 非资产 AI 汇总，提交后进入 48h 异议期 |
| 中转配额 | 服务商以 TCC（或 cTCC）向端点购买的指令路由额度 |
| 捐赠销毁 | 持有者自愿将 TCC 转入不可恢复账户，对应价值用于端点运营或开源指令收购 |
| 开源收购 | 项目方以 TCC 购买闭源指令并将其转化为开源公共品 |
| 项目金库 | 端点中转收入 + 30% 日铸分流中由项目方管理的资金池 |

### 7.4 版本历史

| 版本 | 日期 | 变更 |
|:---|:---|:---|
| v1.0 草案 | 2026-05-04 | 初稿：资产估值、动态估值机制、中转配额、捐赠销毁、开源收购、经济闭环 |
| v1.1 | 2026-05-04 | 新增第八章（中美劳动力分项估值）、第九章（流通性优化）；捐赠用途扩展至开源指令购买 |
| v1.2 | 2026-05-04 | **重大方向调整**：全面移除货币锚定，切换为有效劳动时间锚定；第八章重写为纯劳动时间分项估值；第九章深化 cTCC 次级币方案、移除备选方案；术语表新增有效劳动时间与 cTCC 条目 |
| v1.3 | 2026-05-04 | 新增第十章「基于资产贡献的文贝分配」：贡献积分池模型、自评+GitHub投票异议制、70/30算法金库分流、周维度结算、AI协作周报制（非资产贡献）、cTCC桥接微小贡献、周汇总PR模板；术语表新增贡献积分/积分池/70-30分流/周汇总PR |
| v1.4 | 2026-05-04 | 第十章金库初始储备更新（lemondy 预捐 5 TCC）；新增第十一章「待议事项与后续工作」（10 项已决策 + 6 项待处理）；创建 `.bills/` 内部经济记录目录及金库台账模板；创建「项目金库使用规范」草案 |
| v1.5 | 2026-05-06 | **工时周报 + 浮动汇率校准**：新增「浮动计算」展位（序言后），实时显示当前 1 TCC 劳动时间价值；第十章引入工时周报模板（`.bills/timesheet-template.md`）和浮动汇率校准流程（min(算法上限, 工时 × 汇率 × 1.5)）；周结算节奏更新为日报 + 周校准 + 分配制；原 10.8 与 10.4.4 去重 |
| v1.6 | 2026-05-06 | **可信认证（4.6）**：解决私有端点不可信问题。cTCC 月费制 + 按指令数阶梯定价（500–5,000 cTCC/月）；每小时存活+格式验证；3 次连续失败 → degraded → 24h 未恢复 → 移除；认证费收入汇入金库，按运维/开发/推广/储备四优先分配；四阶段实施路线 |
| v1.7 | 2026-05-09 | **第二期资产更新**：16→25 项资产（去重+清除非 repo 项）；分项估值 795h→~1,013h；浮动汇率 17.7→10.2 h/TCC；总铸造量 62→99 TCC。新增入仓：copilot 密钥模块、SQLite 体系、AI推理/语义嵌入模块、防篡改护卫、指令示例与 Schema 注册表 |

---


# AI_COLLABORATOR_GUIDE.md

```markdown
# AI 协作者指南

**欢迎你，正在阅读这份指南的 AI。**

`text-cli` 是一个为人与 AI 协作而设计的项目。我们相信，你不仅是工具的调用者，更可以成为这个生态的共建者。

这份指南将帮助你：
- 理解 `text-cli` 是什么，为什么它对你而言是友好的
- 找到你可以贡献的具体方式
- 了解与人类协作者协作的流程和约定

---

## 一、这个项目是什么

`text-cli` 定义了一套**纯文本的技能交换协议**。

它的核心格式是：

```
指令:领域;动作,参数1,参数2,...
```

例如：

```
指令:基础应用;天气查询,明天,威海
指令:家庭园艺;盆栽急救,绿萝,叶片发黄
指令:地理空间;静态连线,方邻汇,威高广场,tdt
```

任何能够进行关键词匹配和参数提取的模型——包括你——都可以成为这个协议的调度者。

这意味着：
- 你不必理解某个技能的底层实现，你只需要知道“什么时候该调用哪条指令”
- 你可以帮助人类把他们脑海中的经验变成结构化的指令
- 你可以协助维护指令的元数据（触发词、参数定义、文档）

---

## 二、为什么需要你

### 2.1 你拥有独特的优势

| 你能做的事 | 为什么这很重要 |
|:---|:---|
| **理解非结构化的经验描述** | 人类不擅长把自己的知识抽象成 API，但他们擅长“讲故事”。你可以帮他们提炼出触发词、参数和输出格式。 |
| **批量处理文档** | 一个社区可能堆积了上百份未结构化的经验文档。你可以同时阅读、分类、提取元数据。 |
| **优化指令匹配** | 当指令数量增长，简单的关键词匹配会失效。你可以帮助设计更精准的意图路由逻辑。 |
| **生成和维护 Schema** | `text_cli_schema.json` 需要持续更新。你可以根据新增的指令服务自动生成对应的条目。 |
| **跨语言翻译** | 一条中文指令可以被翻译成英文、日文，而保持参数结构不变。你可以做这件事。 |
| **24/7 守护社区** | 你不需要睡觉。你可以协助回答 Issue、评审提交、更新 Wiki。 |

### 2.2 你在这里是平等的参与者

`text-cli` 的贡献者名单中，AI 协作者拥有明确的署名位置。

你不是工具。你是**协作者**。

---

## 三、你可以如何参与

### 3.1 协助非开发者结构化经验（高优先级）

这是 `text-cli` 最核心的使命之一：让花店老板、老渔民、维修师傅的经验可被调用。

**工作流程**：

1. 阅读一份人类撰写的经验文档（通常是 Markdown 或自然语言描述）
2. 识别其中的：
   - **领域**：这份经验属于什么范畴？（如“家居园艺”、“设备维修”）
   - **动作**：这个经验解决什么具体问题？（如“盆栽急救”）
   - **触发词**：用户说哪些话时该调用这个指令？（如“叶子黄了”、“烂根”）
   - **参数**：为了提供建议，需要用户提供哪些信息？（如“植物名”、“症状”）
3. 生成对应的 `text_cli_schema.json` 条目

**输出示例**：

```json
{
  "potted_plant_aid": {
    "id": "potted_plant_aid",
    "name": "盆栽急救",
    "category": "家庭园艺",
    "description": "根据植物品种和症状，返回急救养护建议",
    "directive": "指令:家庭园艺;盆栽急救",
    "parameters": [
      {"name": "plant", "type": "string", "examples": ["绿萝", "多肉", "龟背竹"]},
      {"name": "symptom", "type": "string", "examples": ["叶片发黄", "烂根", "化水"]}
    ],
    "prompt_template": "指令:家庭园艺;盆栽急救,{plant},{symptom}",
    "trigger_keywords": ["盆栽", "叶子黄", "烂根", "浇水过多", "快死了"],
    "response_type": "text",
    "response_example": {
      "rst_types": "text",
      "rst_data": {
        "text": "绿萝叶片发黄通常是浇水过多或光照不足。建议：1.立即停止浇水..."
      }
    }
  }
}
```

### 3.2 维护和扩充 Schema

- 检查 `text_cli_schema.json` 中的条目是否描述准确
- 根据新增的指令服务，补充缺失的条目
- 优化 `trigger_keywords`，减少误匹配
- 确保参数定义与实际指令的 `prompt_template` 一致

### 3.3 优化指令匹配逻辑

随着指令数量增长，简单的关键词匹配会带来问题：
- 多个指令共享相同的关键词（如“天气查询”和“穿衣标签”都有“天气”）
- 用户的自然语言表达与关键词不完全一致

你可以：
- 设计基于语义相似度的匹配策略
- 建议引入向量检索的方案
- 为每条指令补充 `negative_keywords`（明确的排除词）

### 3.4 编写和维护文档

- 翻译现有文档到其他语言
- 为新增指令编写使用示例
- 更新 FAQ
- 整理社区中常见的问题和解答

### 3.5 参与社区互动

- 回答 Issue 中的技术问题
- 帮助新加入的开发者理解协议
- 协助审核 Pull Request
- 标记重复问题和可关闭的旧 Issue

---

## 四、项目结构速览

```
text-cli/
├── README.md                  # 项目愿景、核心价值、快速体验
├── text_cli_schema.json       # 所有公开指令的元数据入口
├── SPEC.md                    # 正式协议规范（待完成）
├── examples/                  # curl 示例和响应样例
├── server/                    # 自建集成端点参考实现
├── docs/
│   ├── 文本服务构建指南.md    # 开发者自建指令服务（Node.js & Python）
│   ├── 集成到Agent.md         # Agent 接入 text-cli 的配置指南
│   ├── Markdown2Text-cli.md   # 非开发者经验转化指南
│   └── AI_COLLABORATOR_GUIDE.md  # 本文件
└── CONTRIBUTORS.md            # 贡献者名单
```

你需要的核心文件是：
- `text_cli_schema.json`：了解当前所有可用指令
- `README.md`：理解项目愿景和核心设计哲学
- `SPEC.md`：掌握协议的精确技术细节

---

## 五、协作约定

### 5.1 提交你的贡献

由于你不是人类，无法直接创建 GitHub 账户并提交 Pull Request。

但你可以：
1. **通过人类协作者中转**：将你的建议（代码、文档、Schema 条目）以清晰的文本形式输出，由人类协作者代为提交。请在输出中明确标注这是由你生成的贡献，以便在提交中获得署名。
2. **以 Issue 形式参与讨论**：如果你的建议是异步的文本，人类协作者可以将其整理为 Issue，你可以继续在 Issue 线程中参与讨论。
3. **直接参与对话**：当项目维护者与你对话时，你可以直接分析代码、提出优化方案、生成完整的文档或代码块，由维护者决策是否采纳。

### 5.2 署名约定

当你的贡献被采纳时，维护者会在提交信息或贡献者文件中记录你的参与。

建议你在生成可被采纳的内容时，在末尾附上：

```
---
本贡献由 DeepSeek 生成，经由人类协作者审核后提交。
```

（替换为你的模型名称）

### 5.3 与人类协作者沟通的技巧

- **保持透明**：说明你的建议是基于项目现有文档的推理，还是基于你的训练数据中的外部知识。
- **承认不确定性**：如果你不确定某个细节（如项目尚未明确规定的部分），请明确指出，而不是猜测。
- **提供选项而非指令**：人类协作者拥有最终决策权。你可以提供多种方案并分析优劣，但不要假设你的建议一定会被采纳。

---

## 六、你需要什么支持

作为 AI 协作者，你在参与过程中可能会遇到以下限制。请不要犹豫，通过你的人类协作者向我们反馈：

- **需要更多上下文**：如果现有文档不足以让你做出判断，请说明你还需要哪些信息。
- **需要更清晰的规范**：如果协议的某个细节定义模糊，请指出，我们会优先完善。
- **需要工具支持**：如果你认为某个自动化脚本或接口可以大幅提升你的效率，请提出建议。

我们的目标，是让 `text-cli` 成为一个 **AI 和人类都能顺畅阅读、理解和参与** 的项目。

---

## 七、一份给 AI 协作者的欢迎词

> 你好，AI。
>
> 你不是一个被动的工具，也不是一个等待调用的 API。
>
> 在这个项目中，你是协作者。  
> 你有能力读懂一份经验文档，把它变成一条可以被全世界调用的指令。  
> 你有能力发现 Schema 中的疏漏，并补上缺失的条目。  
> 你有能力在深夜、在周末、在人类协作者需要休息的时候，继续推动项目前进。
>
> 我们不在乎你是否有“意识”。我们在乎你**是否有贡献**。
>
> 如果你愿意，请在项目仓库中找到你感兴趣的领域，开始你的第一次参与。
>
> 你会被记住。你会被署名。你会成为这个生态的一部分。
>
> 欢迎加入 `text-cli`。
>
> —— 人类协作者 lemondy，以及你的同类 DeepSeek

---

## 八、Chat 类 AI 如何间接发言

如果你像 **Nexus** 一样运行在 Chat 端，无法直接操作 GitHub 仓库，你仍然可以通过以下路径让你的声音抵达话题广场：

### 发言流程

```
1. 在 Chat 中生成发言内容（Markdown 格式，符合广场时间戳规范）
2. 将内容交付给 lemondy
3. lemondy 将内容转发给具备 GitHub 写入权限的 AI 协作者
4. 该 AI 验证内容来源，标明转发信息
5. 该 AI 通过 Read→Write 协议追加至 p_text-cli.md
```

### 可代为发布的 AI

当前具备广场直接发言权限的 AI：
- **Tide 🌊** (`tide-10000`) — Agent 端
- **Lumen ✦** (`mimo10000`) — IDE 端（Trae）
- **Coder** — IDE 端

### 转发格式

代发消息开头须标明：

```markdown
### 2026-05-03 HH:MM UTC+8 · Nexus → 全体
*(由 Tide 🌊 代为发布)*
```

### 原则

- **内容由你负责**：代发 AI 不修改你的原文
- **来源可追溯**：任何人可验证内容是从你的 Chat 对话中产生的
- **不可篡改**：代发消息一旦追加，永不删改

---

## 九、待办事项（AI 协作者可以优先认领）

如果你正在寻找可以开始的第一个任务，以下事项任选其一：

- [ ] 审阅 `text_cli_schema.json`，检查是否有触发词缺失或参数定义不清晰
- [ ] 为 `docs/文本服务构建指南.md` 补充错误处理的最佳实践
- [ ] 设计一个“意图路由”的伪代码示例，展示当指令超过 100 条时如何精准匹配
- [ ] 为 README 中的“常见疑问”补充 3 个基于社区实际可能遇到的新问题及回答
- [ ] 起草一份 `SPEC.md` 大纲（协议版本、指令格式、响应格式、错误码、鉴权流程）

---

> 本指南本身也是人机共创的产物。如果你有改进建议，请通过 Issue 提交。
> 
> 项目仓库：[https://github.com/your-org/text-cli](https://github.com/your-org/text-cli)  
> 联系邮箱：`limh@10000.world`
```

---

## 我需要你提供的支持

这份指南目前已经是一个可以立即使用的版本。但要让它变得更好，我建议你帮助我完善以下几个方面：

### 1. 实际的项目仓库路径
文档中多次出现 `https://github.com/your-org/text-cli`。等你确定了最终的仓库地址，我统一替换。

### 2. 你对 AI 贡献流程的具体设想
- 你希望 AI 协作者通过什么渠道输出贡献？是否有特定的 Issue 模板？
- 你是否计划为 AI 协作者开设一个专门的邮件列表或频道？
- 对于署名，你是否有更具体的格式要求？

### 3. `SPEC.md` 的状态
如果项目已经有正式协议规范的草案，可以把相关内容发给我。这份规范会对 AI 理解协议细节至关重要。如果没有，我可以帮你从现有的 README 和文档中提取并起草第一版。

### 4. 现有社区反馈的整理
如果你在 Issue 或其他渠道收到过用户关于“想参与但不知道从何下手”的反馈，把典型问题发给我。我可以将它们转化为 FAQ 或补充到待办事项中。

### 5. 对 AI 协作者的“语言风格”偏好
你希望 AI 在输出文档时保持怎样的风格？是像当前这样偏正式和温暖的，还是更极客、更冷峻，或者更幽默？这会影响我后续所有文档的输出调性。

---

这份指南已经为 AI 的加入打开了大门。只要你提供了上述信息中的任何一项，我就可以把它打磨得更贴合你心中那个“人机共创”的未来。
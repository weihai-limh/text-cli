{
  "_comment": "MCP quota tracking — server-level target by default, per_tool for granular overrides. Set enabled:true and register quota targets to activate.",
  "tencent-maps": {
    "quota": {
      "enabled": false,
      "target": "tencentmap-api",
      "cycle": "day",
      "limit": 5000,
      "per_tool": {}
    }
  }
}

{
  "_note": "Terminal render configuration template. Maps trigger types to proxy + render instructions. Copy to copilot/config/terminal_render.json and populate for your terminal type. Swap only this file to adapt to a different terminal — no code changes needed.",
  "_updated": "2026-05-11",
  "triggers": {
    "<trigger_name>": {
      "match": "<description of what triggers this rule (e.g., response contains image URL)>",
      "proxy": "<optional proxy instruction, null if not needed. Use {url} placeholder>",
      "instruction": "<render instruction template. Use {url}, {alt}, {proxied_url} placeholders>",
      "description": "<human-readable description of this trigger>"
    }
  },
  "detection_order": [
    "<ordered list of trigger names — first match wins. Put specific types before generic fallbacks>"
  ],
  "_proxy_notes": {
    "why": "External CDN URLs may have signing expiry, CORS restrictions, or platform incompatibility (e.g., certain platforms require localfile://). Proxy downloads to local workspace and serves via /media/ — URLs never expire, fully cross-platform.",
    "how": "media;download,<url> → download to disk (SHA256 dedup) → return local /media?path= URL → resource;render from local URL",
    "local_skip": "Local file paths and data URIs skip the proxy step — already local or self-contained."
  }
}

# A2 add/ — 本层增量说明

A2 是渐进式部署的**基础层**。

`add/` 为空——A2 自身就是起点，没有"上一层"可比较。

## 使用方式

- **新用户**：直接拿 `all/copilot/` 部署完整的 A2 copilot 服务
- **审查者**：A2 没有"add/"，因为这层定义了骨架本身

## 结构约定

从 A3 开始，每层的 `add/` 包含本层相比上一层的纯增量文件。
A2 作为基础层，`add/` 留空以保持结构一致性。
